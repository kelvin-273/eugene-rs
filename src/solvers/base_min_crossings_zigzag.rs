struct Segment<T> {
    start: usize,
    end: usize,
    data: T,
}

fn min_covering_segments<T>(n_loci: usize, semgents: Vec<Segment<T>>) -> Vec<Segment<T>> {
    unimplemented!()
}

fn min_cover_segments_from_groups<T>(
    n_loci: usize,
    semgent_groups: Vec<Vec<Segment<T>>>,
) -> Vec<Vec<Segment<T>>> {
    unimplemented!();
}

pub fn breeding_program<A, B>(n_loci: usize, pop_0: Vec<A>, ideotype: A) {
    unimplemented!();
}
